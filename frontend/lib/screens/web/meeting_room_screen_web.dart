import 'package:flutter/material.dart';

class MeetingRoomScreenWeb extends StatelessWidget {
  const MeetingRoomScreenWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Meeting Room'),
        automaticallyImplyLeading: false,
      ),
      body: const Center(
        child: Text('Meeting Room Screen - Coming Soon'),
      ),
    );
  }
}

