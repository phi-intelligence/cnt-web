import 'package:flutter/material.dart';

class JoinPrayerScreenWeb extends StatelessWidget {
  const JoinPrayerScreenWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      resizeToAvoidBottomInset: false,
      appBar: AppBar(
        title: const Text('Join Prayer'),
        automaticallyImplyLeading: false,
      ),
      body: const Center(
        child: Text('Join Prayer Screen - Coming Soon'),
      ),
    );
  }
}

